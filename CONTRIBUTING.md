# コントリビューションガイドライン

img2txt プロジェクトへのコントリビューションにご興味をお持ちいただきありがとうございます。

## セキュリティガイドライン

本リポジトリは公開リポジトリです。意図しない認証情報やシークレットの漏洩を防ぐため、以下のルールを遵守してください。

1. **シークレットのハードコード禁止**
   - API キー、パスワード、認証トークンなどは絶対にソースコードに直接記述しないでください。
   - インフラストラクチャのエンドポイント（例: Cloud Run の `*.run.app` URL、Cloudflare Pages の `*.pages.dev` URL、Neon Postgres エンドポイント、Workload Identity Federation リソース名など）も直接コードに記述してはいけません。
   - 必要な場合は、環境変数またはダミー値（例: `<REDACTED>`）を使用してください。

2. **ローカル環境での `pre-commit` セットアップ必須**
   - コミット前にシークレットの混入を自動的に検知・ブロックするため、開発を開始する前に必ず `pre-commit` をインストールしてください。
   - 詳細は [docs/security/leak-prevention.md](docs/security/leak-prevention.md) をご参照ください。

3. **脆弱性の報告**
   - セキュリティに関する問題や、万が一シークレットの漏洩を発見した場合は、公開 Issue で報告せず、リポジトリ管理者へ直接プライベートな手段でご連絡いただくか、GitHub の Private vulnerability reporting をご利用ください。
