## 背景

本リポジトリは GitHub Actions を CI/CD の中核として利用しており、ワークフローファイル内に不適切な変数インジェクションや過剰な権限設定が混入すると、シークレット漏洩や不正アクセスの原因となります。現在、PR や Push 時の構文チェックとして `actionlint` が導入されていますが、定期的な監査が不足していました。

## このPRで導入するもの

- ツール名: 既存の `actionlint` (v1.7.7)
- 導入箇所: `.github/workflows/actionlint.yml` の `schedule` トリガー追加と `docs/security/leak-prevention.md` への追記
- 期待される効果: 毎週のスケジュール実行により、GitHub Actions の仕様変更や新たな推奨ルールに対して、過去のワークフローがセキュアな状態を保っているかを継続的かつ自動で監査し、シークレット漏洩リスクを未然に防ぎます。

## 検知漏れリスクと補完策

- 検知できないケース: actionlint の静的解析ルールではカバーされない、動的に生成・参照される難読化されたシークレットや、外部 API 側での認証不備。
- 補完策: 既存の TruffleHog や gitleaks のスケジュールスキャン、および CodeQL による JavaScript/TypeScript セマンティック解析との多層防御で補完します。

## マージ前に必要な手動作業（チェックリスト）

レビュアーは PR をマージする前に必ず以下を実施してください。
本 PR の CI は手動作業完了を前提に通る設計です。

- [ ] 本PRの変更が、他の定期監査（Gitleaks/TruffleHog/Trivy）のスケジュール（日曜 19:00〜21:00 UTC）と過度に競合していないことを確認（本PRは日曜 18:00 UTC に設定）。

## マージ後の確認手順

- [ ] 次の月曜 03:00 JST (日曜 18:00 UTC) に `actionlint` ワークフローが自動トリガーされることを確認。
- [ ] 実行結果が green であることを確認。

## ロールバック手順

過剰検知や Actions ランナーの消費が問題になった場合は、`.github/workflows/actionlint.yml` から `schedule` ブロックを削除する PR を作成してマージしてください。

## 参考情報

- 公式ドキュメント: https://github.com/rhysd/actionlint
- 比較検討した他案: 新たな静的解析ツールの導入も検討しましたが、既存で動作している actionlint にスケジュール実行を追加することが最も低コストで確実と判断しました。
- 直近の関連 PR / Issue: なし
