## 背景

公開リポジトリにおいて、意図しないインフラ情報（内部ドメイン等）や、認証情報を含むHTTP/HTTPS URL、またはAIエージェントが生成しがちなプレースホルダーの一時値がコミット・プッシュされる漏洩リスクがありました。本PRでは既存の検知ツールを活用し、これらの未カバー領域に対する水際対策とCIでの検知を強化します。

## このPRで導入するもの

- ツール名: gitleaks カスタムルール (既存ツールの設定拡張)
- 導入箇所: `.gitleaks.toml` および `docs/security/leak-prevention.md`
- 期待される効果: コミット前（ローカル）およびCI上で、内部ドメイン (`*.internal`, `*.corp` など)、Basic認証情報付きURL (`https://user:pass@domain`)、および拡張されたAIプレースホルダー (`CHANGE_ME` 等) を自動検知してブロックします。

## 検知漏れリスクと補完策

- 検知できないケース: 独自の非標準フォーマットのトークンや、ルール外のマイナーな内部ドメイン名など。
- 補完策: 引き続き GitHub Secret Scanning と Push Protection を二重の防御層として活用し、定期的なルール見直しを行います。

## マージ前に必要な手動作業（チェックリスト）

レビュアーは PR をマージする前に必ず以下を実施してください。
本 PR の CI は手動作業完了を前提に通る設計です。

- [x] 新規追加されたルールについて、不要な誤検知が発生しないことを確認する
- [ ] 各開発者に対して、最新の `.gitleaks.toml` をローカルへプルするよう周知する
- [ ] GitHub リポジトリ設定の `Code security` にて、Push protection が有効であることを確認する

## マージ後の確認手順

- [ ] マージ後の `main` ブランチでの push トリガーによる CI (gitleaks workflow) が green になることを確認する
- [ ] ローカル環境でテスト用の意図的なシークレットをコミットした際に、適切に pre-commit フックでブロックされることを確認する

## ロールバック手順

- 新規追加したカスタムルールにより業務に支障をきたす深刻な誤検知が発生した場合、本PRのコミットをリバート（`git revert`）してマージしてください。

## 参考情報

- 既存の漏洩防止の全体像: [docs/security/leak-prevention.md](./docs/security/leak-prevention.md)
- 比較検討した他案: 新規に `detect-secrets` などの別ツールを追加する案もありましたが、既存の gitleaks の機能を最大限に引き出し、運用をシンプルにする方針を選択しました。
