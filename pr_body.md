## 背景

公開リポジトリにおいて、ローカルの認証情報やSSH鍵が意図せずコミットされるリスクがありました。`.gitleaks.toml` によるカスタムルールのブロックは存在しましたが、誤って staging されやすい状態が残っていました。本PRでは水際対策を強化し、これらのファイルが Git に追跡されたり、エディタ上で誤って操作されたりするのを防ぎます。

## このPRで導入するもの

- ツール名: `.gitignore`, `.gitattributes`, `.vscode/settings.json` の設定拡張
- 導入箇所: `.gitignore`, `.gitattributes`, `.vscode/settings.json`, `docs/security/leak-prevention.md`
- 期待される効果: コミット前（ローカル）において、SSH鍵（`id_rsa` 等）、ローカル認証・クラウド設定（`.npmrc`, `.kube/config` 等）、キーストア類が Git の追跡対象から外れ、万が一追加されても diff が表示されなくなります。また、VS Code 上で非表示になり、AIエージェントへの意図せぬコンテキスト混入を防ぎます。

## 検知漏れリスクと補完策

- 検知できないケース: リストアップされていない独自の秘匿ファイルや、標準外の名前を持つ鍵ファイル。
- 補完策: 既存の `gitleaks` フックや GitHub Secret Scanning と組み合わせた多重防御でカバーします。

## マージ前に必要な手動作業（チェックリスト）

レビュアーは PR をマージする前に必ず以下を実施してください。
本 PR の CI は手動作業完了を前提に通る設計です。

- [x] 除外設定が既存の開発ワークフローを阻害しないことを確認する
- [ ] 開発者に対して、最新の設定をローカルへプルするよう周知する

## マージ後の確認手順

- [ ] ローカル環境でテスト用の秘匿ファイル（例: `.npmrc`）を作成した際に、Git の untracked files に表示されないことを確認する
- [ ] VS Code のエクスプローラから上記のファイルが非表示になっていることを確認する

## ロールバック手順

- 誤って必要なファイルが除外されてしまった場合は、本PRのコミットをリバート（`git revert`）するか、該当する設定ファイルから除外エントリを削除してください。

## 参考情報

- 漏洩防止の全体像: `docs/security/leak-prevention.md`
- 比較検討した他案: 新たにローカルスキャン専用の有償ツール等を導入する案もありましたが、無料で利用可能な Git 標準機能とエディタ設定を最大限活用する方針を選択しました。
